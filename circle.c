#include <stdio.h>
int main()
{
    int r = 3;
    float pi = 3.14;
    printf("\n area of the circle is %f sq m", pi * r * r);
    int h = 4;
    printf("\n volume of th cylinder is %f qubic m ", pi * r * r * h);
    return 0;
}